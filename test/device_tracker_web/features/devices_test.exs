defmodule DeviceTrackerWeb.Features.DevicesTest do
  use DeviceTrackerWeb.FeatureCase, async: true

  describe "GET /devices" do
    test "lists devices in alphabetical order" do
    end

    test "groups devices, with groups sorted from largest to smallest" do
    end
  end

  describe "GET /devices/:id" do
    test "shows information for a single device" do
    end

    test "should not show data for a device if it is marked as off" do
    end
  end
end
